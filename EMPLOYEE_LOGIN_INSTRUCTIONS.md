# تعليمات تسجيل دخول الموظفين

## 🔐 بيانات تسجيل الدخول

### **الموظفين المتاحين:**

#### **1. محمد عبد الله**
- **رقم البصمة**: `1`
- **اسم البصمة**: `محمد عبد الله`
- **كلمة المرور**: `123456`

#### **2. عصام**
- **رقم البصمة**: `111`
- **اسم البصمة**: `عصام`
- **كلمة المرور**: `123456`

### **✅ تم إصلاح المشاكل:**
- ✅ **كلمات المرور**: تم تحديثها لاستخدام Bcrypt
- ✅ **تحويل البيانات**: رقم البصمة يتم تحويله إلى رقم
- ✅ **اختبار النظام**: تم اختبار جميع الوظائف

---

## 📱 كيفية الاستخدام

### **1. الوصول لصفحة تسجيل الدخول**
```
https://your-domain.com/mobile/employee-login
```

### **2. تسجيل الدخول**
1. **أدخل رقم البصمة**: `1` أو `111`
2. **أدخل اسم البصمة**: `محمد عبد الله` أو `عصام`
3. **أدخل كلمة المرور**: `123456`
4. **اضغط تسجيل الدخول**

### **3. تسجيل البصمة**
بعد تسجيل الدخول بنجاح، سيتم توجيهك تلقائياً لصفحة تسجيل البصمة:
```
https://your-domain.com/mobile/attendance
```

---

## 🧪 للاختبار

### **1. صفحة الاختبار**
افتح ملف `test_employee_attendance.html` واختبر:
- تسجيل الدخول
- تسجيل البصمة
- جميع الـ API endpoints

### **2. اختبار مباشر**
1. اذهب إلى `/mobile/employee-login`
2. استخدم البيانات أعلاه
3. اختبر تسجيل البصمة

---

## ⚠️ ملاحظات مهمة

### **1. كلمات المرور**
- تم تحديث كلمات المرور لاستخدام خوارزمية Bcrypt
- كلمة المرور الافتراضية: `123456`
- يمكن تغييرها من قاعدة البيانات

### **2. الأمان**
- النظام محمي بالـ middleware
- يتطلب تسجيل دخول لجميع العمليات
- البيانات محمية ومشفرة

### **3. قاعدة البيانات**
- البيانات موجودة في جدول `employees`
- الحقول: `finger_print_id`, `finger_print_name`, `password`
- الحالة: `مفعل` (نشط)

---

## 🔧 إضافة موظف جديد

### **1. إضافة في قاعدة البيانات**
```sql
INSERT INTO employees (
    name, 
    finger_print_id, 
    finger_print_name, 
    password, 
    status
) VALUES (
    'اسم الموظف',
    123,
    'اسم البصمة',
    '$2y$10$...', -- Hashed password
    'مفعل'
);
```

### **2. تشفير كلمة المرور**
```php
use Illuminate\Support\Facades\Hash;

$password = Hash::make('123456');
```

---

## 📞 الدعم

### **في حالة وجود مشاكل:**
1. **تحقق من البيانات**: تأكد من صحة رقم البصمة واسم البصمة
2. **تحقق من كلمة المرور**: استخدم `123456`
3. **تحقق من الحالة**: تأكد أن الموظف `مفعل`
4. **تحقق من الـ logs**: فحص `storage/logs/laravel.log`

### **للمساعدة:**
- راجع ملف `EMPLOYEE_ATTENDANCE_SYSTEM_README.md`
- استخدم صفحة الاختبار `test_employee_attendance.html`

---

**تاريخ التحديث**: 16 يناير 2025  
**الحالة**: جاهز للاستخدام  
**كلمة المرور الافتراضية**: `123456`
