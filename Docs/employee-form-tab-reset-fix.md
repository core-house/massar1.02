# إصلاح مشكلة إعادة تعيين التاب عند إنشاء موظف جديد

## 🔍 المشكلة

عند إنشاء موظف جديد وحفظه، ثم إنشاء موظف آخر، كانت التاب النشطة هي التاب الأخيرة التي كان المستخدم فيها عند حفظ الموظف السابق.

## 🔎 السبب

الكود كان يحفظ التاب النشطة في `localStorage` ويستعيدها عند تحميل الصفحة، بغض النظر عن حالة النموذج (إنشاء أو تعديل).

## ✅ الحل المطبق

تم تعديل الكود لتمييز بين وضع الإنشاء والتعديل:

### 1. عند إنشاء موظف جديد (`isEdit = false`):
- مسح التاب المحفوظة في `localStorage`
- تعيين التاب الأولى (البيانات الشخصية) كتاب نشطة افتراضياً

### 2. عند تعديل موظف (`isEdit = true`):
- استعادة التاب المحفوظة من `localStorage`
- إذا لم تكن هناك تاب محفوظة، استخدام التاب الأولى

## 📋 التغييرات المطبقة

### 1. تحديث `employee-form.blade.php`:
```javascript
// قبل
const savedTab = localStorage.getItem('employeeFormActiveTab');
if (savedTab) {
    this.activeTab = savedTab;
    // ...
}

// بعد
if (!this.isEdit) {
    // Clear saved tab for new employee
    localStorage.removeItem('employeeFormActiveTab');
    this.activeTab = 'personal';
    // ...
} else {
    // Restore active tab from localStorage for edit mode
    const savedTab = localStorage.getItem('employeeFormActiveTab');
    // ...
}
```

### 2. تمرير `isEdit` إلى الـ Blade template:
- تم تمرير `$isEdit` من `form-layout.blade.php` إلى `employee-form.blade.php`
- تم استخدام `@js($isEdit ?? false)` في Alpine.js

## 🎯 النتيجة

الآن:
- ✅ عند إنشاء موظف جديد، التاب الأولى (البيانات الشخصية) هي النشطة افتراضياً
- ✅ عند تعديل موظف، التاب المحفوظة تُستعاد كما هو متوقع
- ✅ تجربة مستخدم أفضل وأكثر وضوحاً

## 💡 الملفات المحدثة

1. `resources/views/livewire/hr-management/employees/partials/form/employee-form.blade.php`
   - إضافة منطق لتمييز بين وضع الإنشاء والتعديل
   - مسح `localStorage` عند إنشاء موظف جديد

2. `resources/views/livewire/hr-management/employees/partials/layouts/form-layout.blade.php`
   - تمرير `$isEdit` إلى `employee-form.blade.php`

