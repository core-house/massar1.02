# دليل تصحيح مشاكل نموذج الموظف

## 🔍 المشكلة الحالية

عند محاولة حفظ الموظف مع الحقول المطلوبة فقط، لا يتم الحفظ ولا تظهر رسائل الأخطاء.

## ✅ الحلول المطبقة

### 1. إصلاح عرض الأخطاء
تم تحديث `form-layout.blade.php` لعرض أخطاء Livewire بشكل صحيح:
- استخدام `$this->getErrorBag()` لعرض أخطاء Livewire
- دمج أخطاء Laravel و Livewire في عرض واحد

### 2. إضافة Logging
تم إضافة logging في `HandlesEmployeeForm::save()`:
- تسجيل البيانات قبل التحقق
- تسجيل الأخطاء عند فشل التحقق
- تسجيل الأخطاء العامة

### 3. إصلاح status Field
- تم تغيير `status` من `required` إلى `nullable`
- إضافة قيمة افتراضية قبل وبعد التحقق

## 🔧 كيفية التحقق من المشكلة

### 1. فحص Logs
افتح ملف `storage/logs/laravel.log` وابحث عن:
- `Employee form data before validation` - لرؤية البيانات المرسلة
- `Employee form validation failed` - لرؤية أخطاء التحقق
- `Employee save error` - لرؤية الأخطاء العامة

### 2. فحص Console في المتصفح
افتح Developer Tools (F12) وافحص:
- Console tab - للأخطاء JavaScript
- Network tab - لرؤية الطلبات المرسلة والاستجابات

### 3. فحص البيانات المرسلة
في Network tab، ابحث عن الطلب إلى `/livewire/message` وافحص:
- `data.fingerprint` - معرف الـ component
- `data.serverMemo.data` - البيانات المرسلة
- `data.serverMemo.errors` - الأخطاء (إن وجدت)

## 📋 الحقول المطلوبة

1. **name** - الاسم (مطلوب)
2. **email** - البريد الإلكتروني (مطلوب)
3. **phone** - رقم الهاتف (مطلوب)
4. **salary_basic_account_id** - الحساب الرئيسي للمرتب (مطلوب)
5. **status** - الحالة (اختياري، قيمة افتراضية: 'مفعل')

## 🐛 خطوات التصحيح

1. **افتح Developer Tools** (F12)
2. **اذهب إلى Console tab**
3. **حاول حفظ النموذج**
4. **افحص الأخطاء في Console**
5. **اذهب إلى Network tab**
6. **ابحث عن الطلب `/livewire/message`**
7. **افحص الاستجابة للبحث عن الأخطاء**
8. **افحص `storage/logs/laravel.log`**

## 💡 حلول محتملة إضافية

### إذا لم تظهر الأخطاء:
1. تأكد من أن `wire:key` موجود على الـ form
2. تأكد من أن Livewire يعمل بشكل صحيح
3. تحقق من أن JavaScript لا يحتوي على أخطاء

### إذا فشل التحقق بصمت:
1. تحقق من أن الحقول المطلوبة موجودة في الطلب
2. تحقق من أن `salary_basic_account_id` يُرسل بشكل صحيح
3. تحقق من أن `status` له قيمة افتراضية

### إذا فشل الحفظ:
1. تحقق من قاعدة البيانات
2. تحقق من الـ permissions
3. تحقق من الـ rate limiting

