# إصلاح مشكلة التحقق من صحة البيانات في نموذج الموظف

## 🔍 المشكلة

عند محاولة حفظ الموظف مع الحقول المطلوبة فقط:
- الاسم
- البريد الإلكتروني
- رقم الهاتف
- الحساب الرئيسي للمرتب

كان الفورم لا يحفظ رغم تعبئة هذه الحقول.

## 🔎 السبب

الحقل `status` كان مطلوباً (`required`) في قواعد التحقق من الصحة، لكن:
1. الحقل له قيمة افتراضية `'مفعل'` في Trait
2. Livewire قد لا يرسل الحقول التي لها قيم افتراضية إذا لم يتم تغييرها
3. التحقق من الصحة كان يفشل لأن `status` غير موجود في الطلب

## ✅ الحل المطبق

### 1. تغيير قواعد التحقق
تم تغيير `status` من `required` إلى `nullable` في:
- `StoreEmployeeRequest::rules()` (السطر 40)
- `StoreEmployeeRequest::getRules()` (السطر 108)

```php
// قبل
'status' => ['required', 'in:مفعل,معطل,active,inactive'],

// بعد
'status' => ['nullable', 'in:مفعل,معطل,active,inactive'],
```

### 2. إضافة قيمة افتراضية قبل التحقق
تم إضافة كود في `HandlesEmployeeForm::save()` لضمان وجود قيمة لـ `status`:

```php
// Ensure status has a value before validation
if (empty($this->status)) {
    $this->status = 'مفعل';
}
```

### 3. إضافة قيمة افتراضية بعد التحقق
تم إضافة كود لضمان وجود قيمة لـ `status` في البيانات المُتحقق منها:

```php
// Ensure status has a default value if not provided
if (empty($validated['status'])) {
    $validated['status'] = $this->status ?? 'مفعل';
}
```

## 📋 الحقول المطلوبة الآن

1. ✅ **name** - الاسم (مطلوب)
2. ✅ **email** - البريد الإلكتروني (مطلوب)
3. ✅ **phone** - رقم الهاتف (مطلوب)
4. ✅ **salary_basic_account_id** - الحساب الرئيسي للمرتب (مطلوب)
5. ⚪ **status** - الحالة (اختياري، قيمة افتراضية: 'مفعل')
6. ⚪ **salary_type** - نوع الراتب (اختياري، قيمة افتراضية: 'ساعات عمل فقط')

## 🔧 مشاكل إضافية تم حلها

### المشكلة 1: `salary_type` Column Cannot Be Null
كان الحقل `salary_type` مطلوباً في قاعدة البيانات (NOT NULL) لكنه لا يُرسل في البيانات.

### الحل المطبق:
1. ✅ إضافة قيمة افتراضية قبل التحقق: `'ساعات عمل فقط'`
2. ✅ إضافة قيمة افتراضية بعد التحقق في البيانات المُتحقق منها
3. ✅ إنشاء migration جديد لجعل `salary_type` nullable في قاعدة البيانات
4. ✅ تشغيل الـ migration بنجاح

### المشكلة 2: `flexible_hourly_wage` و حقول أخرى Cannot Be Null
كانت الحقول التالية مطلوبة في قاعدة البيانات (NOT NULL) لكنها لا تُرسل في البيانات:
- `flexible_hourly_wage`
- `allowed_permission_days`
- `allowed_late_days`
- `allowed_absent_days`
- `allowed_errand_days`
- `is_errand_allowed`

### الحل المطبق:
1. ✅ إضافة قيم افتراضية قبل التحقق (0 للـ numeric، false للـ boolean)
2. ✅ إضافة قيم افتراضية بعد التحقق في البيانات المُتحقق منها
3. ✅ إنشاء migration جديد لضمان وجود default values في قاعدة البيانات
4. ✅ تشغيل الـ migration بنجاح

## 🎯 النتيجة

الآن يمكن حفظ الموظف مع الحقول المطلوبة فقط دون الحاجة لتعبئة `status`، حيث سيتم استخدام القيمة الافتراضية تلقائياً.

