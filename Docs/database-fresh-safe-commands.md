# 📚 دليل أوامر Database Fresh Safe

## نظرة عامة

أمر `db:fresh-safe` يسمح لك بتنفيذ `migrate:fresh` مع الاحتفاظ ببيانات الجداول المهمة. هذا مفيد جداً عند:
- تحديث هيكل قاعدة البيانات
- إصلاح مشاكل في Migrations
- إعادة بناء قاعدة البيانات مع الحفاظ على البيانات
- **إضافة أعمدة جديدة للجداول الموجودة** - الكوماند يتعامل معها تلقائياً! ✅

---

## 🚀 الأمر الأساسي

### `php artisan db:fresh-safe`

**الوصف:** تنفيذ `migrate:fresh` مع الاحتفاظ ببيانات جميع الجداول (ما عدا الجداول المستثناة افتراضياً).

**ما يفعله:**
1. ✅ إنشاء نسخة احتياطية من البيانات (البيانات فقط بدون structure)
2. ✅ تنفيذ `migrate:fresh` (حذف كل الجداول وإعادة إنشائها من جميع migrations)
3. ✅ استعادة البيانات من النسخة الاحتياطية مع **مطابقة الأعمدة تلقائياً**
4. ✅ الأعمدة الجديدة تُملأ بقيم `NULL` أو `default` تلقائياً
5. ✅ تنظيف الملفات المؤقتة

**متى تستخدمه:**
- عند تحديث Migrations (بما فيها migrations جديدة تضيف أعمدة)
- عند إصلاح مشاكل في هيكل قاعدة البيانات
- عند إعادة بناء قاعدة البيانات من الصفر
- **عند إضافة أعمدة جديدة للجداول الموجودة** - الكوماند يتعامل معها تلقائياً! ✨

**ميزة خاصة:** 🎯
- إذا أضفت migration جديد يضيف أعمدة لجدول موجود، الكوماند سيتعامل معها تلقائياً
- البيانات القديمة تُستعاد، والأعمدة الجديدة تُملأ بقيم `NULL` (إذا كانت nullable) أو بقيم `default`

**مثال:**
```bash
php artisan db:fresh-safe
```

---

## 🎯 كيف يعمل مع Migrations الجديدة؟

### السيناريو الشائع:
لديك جدول موجود (مثلاً `users`) وترغب في إضافة أعمدة جديدة عبر migration جديد.

**قبل التحديث:**
```php
// Migration قديم
Schema::create('users', function (Blueprint $table) {
    $table->id();
    $table->string('name');
    $table->string('email');
});
```

**بعد إضافة Migration جديد:**
```php
// Migration جديد يضيف أعمدة
Schema::table('users', function (Blueprint $table) {
    $table->foreignId('parent_id')->nullable()->after('id');
    $table->string('status')->default('active')->after('email');
});
```

**النتيجة بعد `db:fresh-safe`:**
- ✅ البيانات القديمة تُستعاد بنجاح
- ✅ العمود `parent_id` = `NULL` (لأنه nullable)
- ✅ العمود `status` = `'active'` (لأنه له default)
- ✅ لا حاجة لتعديل البيانات يدوياً!

### كيف يحدث هذا؟
1. **Backup:** يحفظ البيانات القديمة فقط (name, email)
2. **migrate:fresh:** ينشئ الجدول بالبنية الجديدة (id, parent_id, name, email, status)
3. **Restore:** يطابق الأعمدة تلقائياً ويستعيد البيانات

---

## 📋 الخيارات المتاحة

### 1. `--tables` (استثناء جداول معينة)

**الوصف:** استثناء جداول معينة من النسخ الاحتياطي.

**الصيغة:**
```bash
php artisan db:fresh-safe --tables=table1,table2,table3
```

**مثال:**
```bash
# استثناء جداول Cache و Sessions
php artisan db:fresh-safe --tables=cache,sessions,cache_locks
```

**متى تستخدمه:**
- عندما تريد حذف بيانات جداول معينة (مثل Cache)
- عندما تكون الجداول كبيرة ولا تحتاجها
- عندما تريد إعادة تعيين بيانات معينة

**الجداول المستثناة افتراضياً:**
- `migrations` - سجل Migrations
- `failed_jobs` - الوظائف الفاشلة
- `password_reset_tokens` - رموز إعادة تعيين كلمة المرور
- `sessions` - جلسات المستخدمين
- `cache` - بيانات الـ Cache
- `cache_locks` - أقفال الـ Cache

---

### 2. `--seed` (تشغيل Seeders)

**الوصف:** تشغيل Seeders بعد استعادة البيانات.

**الصيغة:**
```bash
php artisan db:fresh-safe --seed
```

**مثال:**
```bash
php artisan db:fresh-safe --seed
```

**ما يفعله:**
- ينفذ `php artisan db:seed` بعد استعادة البيانات
- مفيد لإضافة بيانات أولية جديدة

**متى تستخدمه:**
- عند إضافة Seeders جديدة
- عند تحديث بيانات Seeders
- عند إعادة تعيين البيانات الأولية

---

### 3. `--compress` (ضغط النسخة الاحتياطية)

**الوصف:** ضغط ملف النسخة الاحتياطية لتوفير المساحة.

**الصيغة:**
```bash
php artisan db:fresh-safe --compress
```

**مثال:**
```bash
php artisan db:fresh-safe --compress
```

**ما يفعله:**
- يضغط ملف النسخة الاحتياطية باستخدام GZIP
- يقلل حجم الملف بنسبة 70-90%
- يفك الضغط تلقائياً عند الاستعادة

**متى تستخدمه:**
- مع قواعد البيانات الكبيرة
- لتوفير مساحة التخزين
- عند نقل النسخ الاحتياطية

**ملاحظة:** 
- الملف المضغوط يكون بصيغة `.sql.gz`
- يفك الضغط تلقائياً عند الاستعادة

---

### 4. `--chunk-size` (حجم الدفعة)

**الوصف:** تحديد حجم الدفعة لمعالجة الجداول الكبيرة.

**الصيغة:**
```bash
php artisan db:fresh-safe --chunk-size=5000
```

**القيمة الافتراضية:** `1000`

**مثال:**
```bash
# معالجة 5000 صف في كل مرة
php artisan db:fresh-safe --chunk-size=5000
```

**ما يفعله:**
- يقسم الجداول الكبيرة إلى دفعات
- يعالج كل دفعة على حدة لتوفير الذاكرة
- يعرض شريط تقدم لكل جدول كبير

**متى تستخدمه:**
- مع الجداول التي تحتوي على آلاف الصفوف
- عند مواجهة مشاكل في الذاكرة
- لتسريع العملية (قيم أكبر = أسرع لكن يحتاج ذاكرة أكثر)

**نصائح:**
- **قيم صغيرة (500-1000):** للبيانات الكبيرة جداً أو ذاكرة محدودة
- **قيم متوسطة (2000-5000):** للاستخدام العادي
- **قيم كبيرة (10000+):** للبيانات المتوسطة وذاكرة كافية

---

### 5. `--skip-large` (تخطي الجداول الكبيرة)

**الوصف:** تخطي الجداول التي تحتوي على أكثر من 10,000 صف.

**الصيغة:**
```bash
php artisan db:fresh-safe --skip-large
```

**مثال:**
```bash
php artisan db:fresh-safe --skip-large
```

**ما يفعله:**
- يتخطى الجداول التي تحتوي على أكثر من 10,000 صف
- يحتفظ ببيانات الجداول الصغيرة فقط
- يعرض تحذير لكل جدول تم تخطيه

**متى تستخدمه:**
- عند وجود جداول كبيرة جداً (ملايين الصفوف)
- عندما لا تحتاج بيانات الجداول الكبيرة
- لتسريع العملية

**تحذير:** ⚠️
- سيتم فقدان بيانات الجداول الكبيرة
- استخدمه فقط إذا كنت متأكداً أنك لا تحتاج هذه البيانات

---

## 🔄 أمثلة استخدام متقدمة

### مثال 1: Fresh Safe عادي
```bash
php artisan db:fresh-safe
```
**الاستخدام:** تحديث Migrations مع الاحتفاظ بكل البيانات

---

### مثال 2: مع Seeders
```bash
php artisan db:fresh-safe --seed
```
**الاستخدام:** Fresh Safe + إضافة بيانات أولية جديدة

---

### مثال 3: استثناء جداول معينة
```bash
php artisan db:fresh-safe --tables=cache,sessions,logs
```
**الاستخدام:** Fresh Safe مع حذف بيانات Cache و Sessions و Logs

---

### مثال 4: للبيانات الكبيرة
```bash
php artisan db:fresh-safe --compress --chunk-size=5000
```
**الاستخدام:** 
- ضغط النسخة الاحتياطية
- معالجة 5000 صف في كل دفعة
- مناسب للجداول الكبيرة

---

### مثال 5: تخطي الجداول الكبيرة
```bash
php artisan db:fresh-safe --skip-large --compress
```
**الاستخدام:**
- تخطي الجداول الكبيرة (أكثر من 10,000 صف)
- ضغط النسخة الاحتياطية
- مناسب عندما لا تحتاج بيانات الجداول الكبيرة

---

### مثال 6: كل الخيارات معاً
```bash
php artisan db:fresh-safe \
  --tables=cache,sessions \
  --seed \
  --compress \
  --chunk-size=3000 \
  --skip-large
```
**الاستخدام:** 
- استثناء Cache و Sessions
- تشغيل Seeders
- ضغط النسخة الاحتياطية
- معالجة 3000 صف في كل دفعة
- تخطي الجداول الكبيرة

---

## 📊 مقارنة الأداء

### بدون تحسينات:
```bash
php artisan db:fresh-safe
```
- ⏱️ **الوقت:** ~2-5 دقائق (حسب حجم البيانات)
- 💾 **الذاكرة:** ~512 MB
- 📦 **حجم النسخة:** ~50-100 MB

### مع تحسينات:
```bash
php artisan db:fresh-safe --compress --chunk-size=5000
```
- ⏱️ **الوقت:** ~1-3 دقائق
- 💾 **الذاكرة:** ~256 MB
- 📦 **حجم النسخة:** ~10-20 MB (مضغوط)

---

## ⚠️ تحذيرات مهمة

### 1. النسخ الاحتياطي
- ✅ **يُنصح بشدة** بعمل نسخة احتياطية يدوية قبل التنفيذ
- ✅ النسخة الاحتياطية التلقائية تُحفظ في `storage/app/backups/`
- ✅ الملفات المؤقتة تُحذف تلقائياً بعد الاستعادة

### 2. قاعدة البيانات الإنتاجية
- ⚠️ **لا تستخدم** هذا الأمر على قاعدة بيانات الإنتاج مباشرة
- ⚠️ اختبر الأمر أولاً على بيئة التطوير
- ⚠️ تأكد من وجود نسخة احتياطية كاملة قبل التنفيذ

### 3. الجداول الكبيرة
- ⚠️ الجداول الكبيرة جداً (ملايين الصفوف) قد تستغرق وقتاً طويلاً
- ✅ استخدم `--compress` لتقليل الوقت
- ✅ استخدم `--chunk-size` لتقليل استهلاك الذاكرة

---

## 🔍 استكشاف الأخطاء

### المشكلة: "mysqldump غير متاح"
**الحل:** 
- الأمر يستخدم الطريقة البديلة تلقائياً (Laravel method)
- تأكد من تثبيت MySQL Client Tools للحصول على أداء أفضل

### المشكلة: "الأعمدة الجديدة لا تظهر بعد الاستعادة"
**الحل:**
- ✅ تم إصلاح هذه المشكلة في الإصدار الحالي
- الكوماند الآن يستخدم `--no-create-info` لحفظ البيانات فقط
- الجداول تُنشأ من migrations (بما فيها الجديدة)
- الاستعادة تطابق الأعمدة تلقائياً
- إذا واجهت المشكلة، تأكد من تحديث الكوماند إلى آخر إصدار

### المشكلة: "Out of Memory"
**الحل:**
```bash
php artisan db:fresh-safe --chunk-size=500
```

### المشكلة: "الملف كبير جداً"
**الحل:**
```bash
php artisan db:fresh-safe --compress
```

### المشكلة: "الاستعادة بطيئة"
**الحل:**
- استخدم `--skip-large` للجداول الكبيرة غير المهمة
- استخدم `--compress` لتقليل حجم الملف

---

## 📝 ملاحظات إضافية

### مكان حفظ النسخ الاحتياطية:
```
storage/app/backups/backup_YYYYMMDDHHMMSS.sql
```

### تنسيق الملفات:
- **غير مضغوط:** `.sql`
- **مضغوط:** `.sql.gz`

### تنظيف النسخ القديمة:
```bash
# حذف النسخ الأقدم من 7 أيام
find storage/app/backups -name "backup_*.sql*" -mtime +7 -delete
```

### كيف يعمل الكوماند مع Migrations الجديدة؟ 🔧

1. **Backup:** يحفظ البيانات فقط (INSERT statements) بدون CREATE TABLE
   - يستخدم `mysqldump --no-create-info --complete-insert`
   - هذا يضمن أن structure يُنشأ من migrations فقط

2. **migrate:fresh:** ينفذ جميع migrations (بما فيها الجديدة)
   - الجداول تُنشأ بالبنية الجديدة مع الأعمدة الجديدة

3. **Restore:** يستعيد البيانات بطريقة ذكية
   - يطابق الأعمدة بين backup والجدول الحالي
   - الأعمدة القديمة: تُستعاد قيمها من backup
   - الأعمدة الجديدة: تُملأ بقيم `NULL` أو `default` تلقائياً
   - الأعمدة المحذوفة: يتم تجاهلها تلقائياً

**مثال عملي:**
```php
// قبل: جدول users به (id, name, email)
// بعد: migration جديد يضيف (parent_id, status)
// النتيجة: البيانات القديمة تُستعاد + parent_id و status = NULL أو default
```

---

## ✅ الخلاصة

| الأمر | الاستخدام | الوقت المتوقع |
|------|----------|---------------|
| `db:fresh-safe` | استخدام عادي | 2-5 دقائق |
| `db:fresh-safe --compress` | بيانات كبيرة | 1-3 دقائق |
| `db:fresh-safe --seed` | مع Seeders | 3-6 دقائق |
| `db:fresh-safe --skip-large` | تخطي الجداول الكبيرة | 30 ثانية - 2 دقيقة |

### ✨ الميزات الجديدة (v2.0):
- ✅ **دعم تلقائي للأعمدة الجديدة** - لا حاجة لتعديل البيانات يدوياً
- ✅ **مطابقة ذكية للأعمدة** - يتعامل مع الأعمدة المضافة/المحذوفة تلقائياً
- ✅ **أداء محسّن** - يستخدم mysqldump للسرعة مع Laravel للدقة

---

## 🎯 أفضل الممارسات

1. ✅ **اختبر أولاً** على بيئة التطوير
2. ✅ **نسخة احتياطية** قبل التنفيذ
3. ✅ **استخدم `--compress`** للبيانات الكبيرة
4. ✅ **استخدم `--chunk-size`** لتقليل الذاكرة
5. ✅ **راقب الأداء** أثناء التنفيذ
6. ✅ **تحقق من النتائج** بعد التنفيذ
7. ✅ **عند إضافة أعمدة جديدة:** استخدم `nullable()` أو `default()` للأعمدة الجديدة لتجنب مشاكل الاستعادة
8. ✅ **بعد الاستعادة:** تحقق من أن الأعمدة الجديدة موجودة وتم ملؤها بالقيم الصحيحة

---

**تم إنشاء هذا الدليل بواسطة:** Laravel Boost AI Assistant  
**التاريخ:** {{ date('Y-m-d') }}  
**الإصدار:** 1.0.0

