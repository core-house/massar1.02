# Services Module - وحدة إدارة الخدمات

وحدة شاملة لإدارة الخدمات في نظام إدارة الإنتاج، تتيح إضافة وإدارة الخدمات المختلفة مع إمكانية حجزها وبيعها للعملاء.

## المميزات الرئيسية

### 🛠️ إدارة الخدمات
- إضافة وتعديل وحذف الخدمات
- تصنيف الخدمات حسب النوع (استشارة، صيانة، إصلاح، تركيب، تدريب، إلخ)
- تحديد أسعار ووحدات قياس مختلفة للخدمات
- إدارة متطلبات ومميزات كل خدمة
- رفع صور للخدمات
- تحديد مدة الخدمة بالدقائق

### 📅 نظام الحجوزات
- حجز الخدمات للعملاء
- تحديد مواعيد الحجوزات
- إدارة الموظفين المسؤولين عن الخدمات
- تتبع حالة الحجوزات (معلق، مكتمل، ملغي)
- إلغاء الحجوزات مع ذكر السبب
- عرض الأوقات المتاحة للحجز

### 🏪 تكامل مع نقاط البيع
- بيع الخدمات من خلال نظام POS
- اختيار الخدمات بسهولة من واجهة نقاط البيع
- إنشاء حجوزات تلقائية عند بيع الخدمات
- دعم وحدات القياس والأسعار المختلفة

## الهيكل التقني

### النماذج (Models)
- **Service**: نموذج الخدمة الرئيسي
- **ServiceBooking**: نموذج حجوزات الخدمات

### الجداول (Database Tables)
- `services`: جدول الخدمات الرئيسي
- `service_bookings`: جدول حجوزات الخدمات
- `service_units`: جدول ربط الخدمات بالوحدات
- `service_prices`: جدول ربط الخدمات بالأسعار
- `service_categories`: جدول ربط الخدمات بالتصنيفات
- `service_items`: جدول ربط الخدمات بالأصناف المطلوبة

### المتحكمات (Controllers)
- **ServiceController**: إدارة الخدمات
- **ServiceBookingController**: إدارة الحجوزات
- **ServicePOSController**: تكامل مع نقاط البيع

### المكونات التفاعلية (Livewire)
- **ServiceSelector**: مكون اختيار الخدمات لنقاط البيع

## التثبيت والإعداد

### 1. تشغيل المايجريشن
```bash
php artisan migrate --path=Modules/Services/database/migrations
```

### 2. تشغيل السيدر (اختياري)
```bash
php artisan db:seed --class=Modules\\Services\\Database\\Seeders\\ServicesSeeder
```

### 3. إضافة المسارات
المسارات مُعرّفة تلقائياً في `RoutesServiceProvider`.

## الاستخدام

### إدارة الخدمات
1. انتقل إلى `/services/services`
2. اضغط "إضافة خدمة جديدة"
3. املأ بيانات الخدمة
4. حدد التصنيفات والوحدات والأسعار
5. احفظ الخدمة

### إدارة الحجوزات
1. انتقل إلى `/services/bookings`
2. اضغط "حجز جديد"
3. اختر الخدمة والعميل
4. حدد التاريخ والوقت
5. احفظ الحجز

### تكامل نقاط البيع
1. في واجهة POS، ستجد تبويب "الخدمات"
2. اختر الخدمة المطلوبة
3. أضفها للفاتورة كأي صنف آخر
4. سيتم إنشاء حجز تلقائي للخدمة

## API Endpoints

### الخدمات
- `GET /services/services` - قائمة الخدمات
- `POST /services/services` - إنشاء خدمة جديدة
- `GET /services/services/{id}` - عرض خدمة محددة
- `PUT /services/services/{id}` - تحديث خدمة
- `DELETE /services/services/{id}` - حذف خدمة

### الحجوزات
- `GET /services/bookings` - قائمة الحجوزات
- `POST /services/bookings` - إنشاء حجز جديد
- `GET /services/bookings/{id}` - عرض حجز محدد
- `PUT /services/bookings/{id}` - تحديث حجز
- `DELETE /services/bookings/{id}` - حذف حجز

### تكامل POS
- `GET /services/pos/services` - خدمات للـ POS
- `GET /services/pos/services/{id}` - خدمة محددة للـ POS
- `POST /services/pos/bookings` - إنشاء حجز من POS
- `GET /services/pos/available-slots` - الأوقات المتاحة

## الاختبار

```bash
# تشغيل اختبارات الوحدة
php artisan test Modules/Services/tests/Feature/ServiceTest.php

# تشغيل جميع اختبارات الوحدة
php artisan test --path=Modules/Services/tests
```

## التخصيص

### إضافة أنواع خدمات جديدة
عدّل enum `service_type` في migration `create_services_table`:

```php
$table->string('service_type')->default('general'); // يمكن إضافة: 'consultation', 'maintenance', 'repair', etc.
```

### تخصيص واجهة المستخدم
عدّل ملفات Blade في `Resources/views/` حسب احتياجاتك.

### إضافة حقول جديدة
1. أضف الحقل في migration
2. عدّل model `Service` أو `ServiceBooking`
3. حدث forms و views

## الدعم والمساعدة

للحصول على المساعدة أو الإبلاغ عن مشاكل:
1. راجع ملفات الاختبار للفهم الأفضل
2. تحقق من logs Laravel
3. تأكد من صحة إعدادات قاعدة البيانات

## الترخيص

هذه الوحدة جزء من نظام إدارة الإنتاج وتخضع لنفس شروط الترخيص.
