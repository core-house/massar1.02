<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | Print Agent URL
    |--------------------------------------------------------------------------
    |
    | عنوان URL لوكيل الطباعة الذي يعمل على Windows
    | يجب أن يكون الوكيل يستمع على هذا العنوان
    |
    */
    'print_agent_url' => env('KITCHEN_PRINT_AGENT_URL', 'http://localhost:5000/print'),

    /*
    |--------------------------------------------------------------------------
    | Request Timeout
    |--------------------------------------------------------------------------
    |
    | المهلة الزمنية (بالثواني) لطلبات HTTP إلى وكيل الطباعة
    |
    */
    'timeout' => env('KITCHEN_PRINT_TIMEOUT', 5),

    /*
    |--------------------------------------------------------------------------
    | Max Retries
    |--------------------------------------------------------------------------
    |
    | عدد المحاولات التلقائية عند فشل الطباعة
    |
    */
    'max_retries' => env('KITCHEN_PRINT_MAX_RETRIES', 3),

    /*
    |--------------------------------------------------------------------------
    | Retry Backoff
    |--------------------------------------------------------------------------
    |
    | فترة الانتظار (بالثواني) بين محاولات إعادة الطباعة
    |
    */
    'retry_backoff' => env('KITCHEN_PRINT_RETRY_BACKOFF', 5),

    /*
    |--------------------------------------------------------------------------
    | Line Width
    |--------------------------------------------------------------------------
    |
    | عرض السطر بالأحرف للطابعات الحرارية
    |
    */
    'line_width' => env('KITCHEN_PRINT_LINE_WIDTH', 32),

    /*
    |--------------------------------------------------------------------------
    | Queue Connection
    |--------------------------------------------------------------------------
    |
    | اتصال قائمة الانتظار المستخدم لمهام الطباعة
    |
    */
    'queue_connection' => env('KITCHEN_PRINT_QUEUE', 'database'),

    /*
    |--------------------------------------------------------------------------
    | Queue Name
    |--------------------------------------------------------------------------
    |
    | اسم قائمة الانتظار لمهام الطباعة
    |
    */
    'queue_name' => env('KITCHEN_PRINT_QUEUE_NAME', 'kitchen-printing'),

    /*
    |--------------------------------------------------------------------------
    | Health Check Interval
    |--------------------------------------------------------------------------
    |
    | فترة التحقق من صحة وكيل الطباعة (بالثواني)
    | يستخدم للكشف السريع عن توقف الوكيل
    |
    */
    'health_check_interval' => env('KITCHEN_PRINT_HEALTH_CHECK_INTERVAL', 60),

    /*
    |--------------------------------------------------------------------------
    | Health Check Timeout
    |--------------------------------------------------------------------------
    |
    | المهلة الزمنية (بالثواني) لطلبات التحقق من الصحة
    |
    */
    'health_check_timeout' => env('KITCHEN_PRINT_HEALTH_CHECK_TIMEOUT', 2),

    /*
    |--------------------------------------------------------------------------
    | Monitoring Retention Days
    |--------------------------------------------------------------------------
    |
    | عدد الأيام للاحتفاظ بسجلات المراقبة
    |
    */
    'monitoring_retention_days' => env('KITCHEN_PRINT_MONITORING_RETENTION_DAYS', 30),

    /*
    |--------------------------------------------------------------------------
    | Success Rate Threshold
    |--------------------------------------------------------------------------
    |
    | الحد الأدنى لمعدل النجاح (%)
    | يتم إرسال تنبيه إذا انخفض المعدل عن هذا الحد
    |
    */
    'success_rate_threshold' => env('KITCHEN_PRINT_SUCCESS_RATE_THRESHOLD', 99.5),

    /*
    |--------------------------------------------------------------------------
    | Dispatch Latency Threshold
    |--------------------------------------------------------------------------
    |
    | الحد الأقصى لزمن الإرسال (بالثواني)
    | يتم إرسال تنبيه إذا تجاوز الزمن هذا الحد
    |
    */
    'dispatch_latency_threshold' => env('KITCHEN_PRINT_DISPATCH_LATENCY_THRESHOLD', 1.0),

    /*
    |--------------------------------------------------------------------------
    | Queue Backlog Threshold
    |--------------------------------------------------------------------------
    |
    | الحد الأقصى لعدد المهام المتراكمة في قائمة الانتظار
    | يتم إرسال تنبيه إذا تجاوز العدد هذا الحد
    |
    */
    'queue_backlog_threshold' => env('KITCHEN_PRINT_QUEUE_BACKLOG_THRESHOLD', 10),
];
