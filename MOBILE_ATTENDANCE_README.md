# نظام تسجيل البصمة للموبايل - دليل الاستخدام

## 📱 نظرة عامة

نظام تسجيل البصمة للموبايل هو نظام متكامل يسمح للموظفين بتسجيل دخولهم وخروجهم من خلال هواتفهم المحمولة مع تحديد الموقع تلقائياً.

---

## 🚀 المميزات

### **1. تسجيل تلقائي للبيانات**
- ✅ **اسم الموظف**: يتم ملؤه تلقائياً من بيانات المستخدم
- ✅ **رقم الموظف**: يتم ملؤه تلقائياً من بيانات المستخدم
- ✅ **رقم البصمة**: يتم ملؤه تلقائياً من بيانات الموظف
- ✅ **اسم البصمة**: يتم ملؤه تلقائياً من بيانات الموظف
- ✅ **التاريخ**: يتم ملؤه تلقائياً بوقت السيرفر
- ✅ **الوقت**: يتم ملؤه تلقائياً بوقت السيرفر
- ✅ **الموقع**: يتم تحديده تلقائياً من GPS

### **2. واجهة مستخدم بسيطة**
- ✅ **تصميم متجاوب**: يعمل على جميع أحجام الشاشات
- ✅ **واجهة عربية**: دعم كامل للغة العربية وRTL
- ✅ **أزرار واضحة**: أزرار كبيرة وسهلة الاستخدام
- ✅ **رسائل تأكيد**: رسائل نجاح وخطأ واضحة

### **3. تحديد الموقع التلقائي**
- ✅ **GPS دقيق**: تحديد الموقع بدقة عالية
- ✅ **عرض العنوان**: تحويل الإحداثيات إلى عنوان
- ✅ **تحديث مستمر**: تحديث الموقع كل 5 دقائق
- ✅ **معالجة الأخطاء**: رسائل خطأ واضحة

---

## 🔧 التثبيت والإعداد

### **1. تشغيل Migration**
```bash
php artisan migrate
```

### **2. إعداد Google Maps API**
```env
# في ملف .env
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
```

### **3. إعداد Routes**
تم إضافة Routes تلقائياً في:
- `routes/web.php`: للصفحة الرئيسية
- `routes/api.php`: لـ API endpoints

---

## 📱 كيفية الاستخدام

### **1. الوصول للصفحة**
```
https://your-domain.com/mobile/attendance
```

### **2. تسجيل البصمة**
1. **اختر نوع البصمة**: دخول أو خروج
2. **اضغط على زر التسجيل**: سيتم تسجيل البصمة تلقائياً
3. **انتظر التأكيد**: ستظهر رسالة نجاح

### **3. البيانات المسجلة تلقائياً**
- **اسم الموظف**: من بيانات المستخدم المسجل دخول
- **رقم الموظف**: من بيانات الموظف المرتبط بالمستخدم
- **رقم البصمة**: من بيانات الموظف أو ID المستخدم
- **اسم البصمة**: من بيانات الموظف أو اسم المستخدم
- **التاريخ**: تاريخ السيرفر الحالي
- **الوقت**: وقت السيرفر الحالي
- **الموقع**: الإحداثيات والعنوان من GPS
- **الحالة**: قيد المراجعة (افتراضياً)

---

## 🛠️ API Endpoints

### **1. تسجيل البصمة**
```http
POST /api/attendance/record
Content-Type: application/json
Authorization: Bearer {token}

{
    "employee_id": 1,
    "employee_attendance_finger_print_id": 123,
    "employee_attendance_finger_print_name": "أحمد محمد",
    "type": "check_in",
    "date": "2025-01-15",
    "time": "09:00",
    "location": "{\"latitude\":30.0444,\"longitude\":31.2357,\"address\":\"القاهرة\"}",
    "status": "pending",
    "notes": "تم التسجيل من الموبايل"
}
```

### **2. الحصول على آخر بصمة**
```http
GET /api/attendance/last?employee_id=1
Authorization: Bearer {token}
```

### **3. الحصول على تاريخ البصمات**
```http
GET /api/attendance/history?employee_id=1&date_from=2025-01-01&date_to=2025-01-31
Authorization: Bearer {token}
```

### **4. الحصول على إحصائيات البصمات**
```http
GET /api/attendance/stats?employee_id=1&date_from=2025-01-01&date_to=2025-01-31
Authorization: Bearer {token}
```

### **5. التحقق من إمكانية تسجيل البصمة**
```http
GET /api/attendance/can-record?employee_id=1&type=check_in
Authorization: Bearer {token}
```

---

## 🗄️ قاعدة البيانات

### **جدول attendances**
```sql
CREATE TABLE attendances (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    employee_id BIGINT UNSIGNED NOT NULL,
    user_id BIGINT UNSIGNED NULL, -- تم إضافته
    employee_attendance_finger_print_id INT NOT NULL,
    employee_attendance_finger_print_name VARCHAR(255) NOT NULL,
    type ENUM('check_in', 'check_out') NOT NULL,
    date DATE NOT NULL,
    time TIME NOT NULL,
    location JSON NULL,
    status ENUM('pending', 'approved', 'rejected') DEFAULT 'pending',
    notes TEXT NULL,
    created_at TIMESTAMP NULL,
    updated_at TIMESTAMP NULL,
    
    FOREIGN KEY (employee_id) REFERENCES employees(id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL,
    INDEX idx_employee_date (employee_id, date),
    INDEX idx_user_id (user_id)
);
```

### **جدول employees**
```sql
-- الحقول المطلوبة موجودة بالفعل
finger_print_id INT NULL,
finger_print_name VARCHAR(255) NULL,
```

---

## 🔒 الأمان

### **1. المصادقة**
- ✅ **Session Authentication**: يتطلب تسجيل دخول
- ✅ **CSRF Protection**: حماية من هجمات CSRF
- ✅ **Rate Limiting**: منع spam requests

### **2. التحقق من البيانات**
- ✅ **Validation**: التحقق من صحة جميع البيانات
- ✅ **Type Checking**: التحقق من نوع البصمة
- ✅ **Date Validation**: التحقق من صحة التاريخ والوقت

### **3. حماية الموقع**
- ✅ **HTTPS**: تشفير البيانات
- ✅ **Location Privacy**: حماية خصوصية الموقع
- ✅ **User Authorization**: التحقق من صلاحيات المستخدم

---

## 📊 التقارير والإحصائيات

### **1. إحصائيات البصمات**
- **إجمالي البصمات**: عدد البصمات في الفترة المحددة
- **دخول**: عدد مرات تسجيل الدخول
- **خروج**: عدد مرات تسجيل الخروج
- **قيد المراجعة**: عدد البصمات المعلقة
- **معتمد**: عدد البصمات المعتمدة
- **مرفوض**: عدد البصمات المرفوضة

### **2. آخر بصمة**
- **النوع**: دخول أو خروج
- **التاريخ**: تاريخ آخر بصمة
- **الوقت**: وقت آخر بصمة
- **الحالة**: حالة آخر بصمة

---

## 🐛 استكشاف الأخطاء

### **1. مشاكل الموقع**
```javascript
// خطأ: "المتصفح لا يدعم تحديد الموقع"
// الحل: تأكد من استخدام HTTPS

// خطأ: "خطأ في تحديد الموقع"
// الحل: تأكد من السماح بالوصول للموقع في المتصفح
```

### **2. مشاكل API**
```javascript
// خطأ: "401 Unauthorized"
// الحل: تأكد من تسجيل الدخول

// خطأ: "422 Validation Error"
// الحل: تأكد من صحة البيانات المرسلة
```

### **3. مشاكل قاعدة البيانات**
```sql
-- خطأ: "Column 'user_id' doesn't exist"
-- الحل: شغل migration
php artisan migrate

-- خطأ: "Foreign key constraint fails"
-- الحل: تأكد من وجود الموظف والمستخدم
```

---

## 🔧 التخصيص

### **1. تغيير التصميم**
```css
/* في resources/views/mobile/attendance.blade.php */
.attendance-card {
    background: white; /* تغيير لون الخلفية */
    border-radius: 20px; /* تغيير شكل الزوايا */
}
```

### **2. تغيير الرسائل**
```javascript
// في resources/views/mobile/attendance.blade.php
const messages = {
    success: 'تم تسجيل البصمة بنجاح',
    error: 'حدث خطأ في تسجيل البصمة',
    locationError: 'خطأ في تحديد الموقع'
};
```

### **3. تغيير API Endpoints**
```php
// في routes/api.php
Route::post('/custom/attendance/record', [MobileAttendanceController::class, 'recordAttendance']);
```

---

## 📱 دعم المتصفحات

### **المتطلبات:**
- **Geolocation API**: لتحديد الموقع
- **Fetch API**: لإرسال البيانات
- **ES6+ Support**: لدعم JavaScript الحديث

### **المتصفحات المدعومة:**
- ✅ Chrome 50+
- ✅ Firefox 44+
- ✅ Safari 11.1+
- ✅ Edge 17+
- ✅ Mobile Browsers

---

## 🚀 التحسينات المستقبلية

### **1. ميزات إضافية**
- **Push Notifications**: إشعارات تذكير
- **Offline Support**: عمل بدون إنترنت
- **Biometric Authentication**: مصادقة بيومترية
- **QR Code**: تسجيل بالكود

### **2. تحسينات الأداء**
- **Caching**: تخزين مؤقت للبيانات
- **Compression**: ضغط البيانات
- **Lazy Loading**: تحميل تدريجي

### **3. تحسينات الأمان**
- **2FA**: مصادقة ثنائية
- **Encryption**: تشفير البيانات
- **Audit Log**: سجل المراجعة

---

## 📞 الدعم الفني

### **للمساعدة:**
1. **تحقق من Console**: فحص أخطاء JavaScript
2. **تحقق من Network**: فحص طلبات API
3. **تحقق من Database**: فحص البيانات المحفوظة
4. **تحقق من Logs**: فحص سجلات Laravel

### **ملفات مهمة:**
- `resources/views/mobile/attendance.blade.php`: الصفحة الرئيسية
- `app/Http/Controllers/MobileAttendanceController.php`: Controller
- `routes/api.php`: API Routes
- `app/Models/Attendance.php`: نموذج البيانات

---

**تاريخ الإنشاء**: 16 يناير 2025  
**الإصدار**: 1.0  
**المطور**: فريق تطوير Massar ERP  
**الحالة**: جاهز للاستخدام
